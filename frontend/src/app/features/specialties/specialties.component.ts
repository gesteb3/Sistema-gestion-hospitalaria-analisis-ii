import { ChangeDetectionStrategy,Component,inject,signal } from '@angular/core';import { FormControl,FormGroup,ReactiveFormsModule,Validators } from '@angular/forms';import { ApiService } from '../../core/api.service';import { apiError } from '../../core/api-error';import { Specialty,SpecialtyPayload } from '../../core/models';
@Component({selector:'app-specialties',imports:[ReactiveFormsModule],template:`
<section class="page-header"><div><span class="eyebrow">Catálogo médico</span><h1>Especialidades</h1><p>Creá, editá y administrá las áreas médicas.</p></div><button class="button primary" (click)="newItem()">+ Nueva especialidad</button></section>@if(message()){<div class="success">{{message()}}</div>}@if(error()){<div class="alert">{{error()}}</div>}<section class="grid">@for(s of items();track s.especialidad_id){<article><div class="mark">+</div><div><h2>{{s.nombre}}</h2><p>{{s.descripcion||'Sin descripción'}}</p><span class="badge" [attr.data-status]="s.activa?'ACTIVO':'INACTIVO'">{{s.activa?'Activa':'Inactiva'}}</span></div><div class="actions-card"><button (click)="edit(s)">Editar</button>@if(s.activa){<button (click)="deactivate(s)">Desactivar</button>}@else{<button (click)="reactivate(s)">Reactivar</button>}</div></article>}</section>
@if(open()){<div class="drawer-backdrop" (click)="close()"><aside class="drawer small" (click)="$event.stopPropagation()"><div class="drawer-title"><div><span class="eyebrow">Catálogo</span><h2>{{selected()?'Editar':'Nueva'}} especialidad</h2></div><button (click)="close()">×</button></div><form class="form-grid" [formGroup]="form" (ngSubmit)="save()"><label class="full"><span>Nombre *</span><input formControlName="nombre"></label><label class="full"><span>Descripción</span><textarea rows="4" formControlName="descripcion"></textarea></label><div class="actions full"><button class="button primary">Guardar</button></div></form></aside></div>}
`,styles:[`.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(310px,1fr));gap:15px;margin-top:20px}.grid article{display:grid;grid-template-columns:45px 1fr auto;gap:12px;padding:18px;border:1px solid var(--border);border-radius:16px;background:#fff}.mark{display:grid;width:45px;height:45px;place-items:center;border-radius:14px;color:#fff;background:linear-gradient(135deg,#155eef,#38bdf8);font-size:1.2rem}.grid h2{margin:2px 0 6px;font-size:.9rem}.grid p{min-height:34px;margin:0 0 9px;color:var(--muted);font-size:.68rem}.actions-card{display:grid;align-content:start;gap:5px}.actions-card button{border:0;color:var(--primary);background:transparent;font-size:.61rem;font-weight:800}.small{max-width:460px}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class SpecialtiesComponent{private api=inject(ApiService);readonly items=signal<Specialty[]>([]);readonly selected=signal<Specialty|null>(null);readonly open=signal(false);readonly error=signal('');readonly message=signal('');readonly form=new FormGroup({nombre:new FormControl('',{nonNullable:true,validators:[Validators.required]}),descripcion:new FormControl('')});constructor(){this.load()}load(){this.api.specialties(true).subscribe({next:v=>this.items.set(v),error:e=>this.error.set(apiError(e))})}newItem(){this.selected.set(null);this.form.reset({nombre:'',descripcion:''});this.open.set(true)}edit(s:Specialty){this.selected.set(s);this.form.reset({nombre:s.nombre,descripcion:s.descripcion||''});this.open.set(true)}save(){if(this.form.invalid)return;const v=this.form.getRawValue();const body:SpecialtyPayload={nombre:v.nombre,descripcion:v.descripcion||null};const call=this.selected()?this.api.updateSpecialty(this.selected()!.especialidad_id,body):this.api.createSpecialty(body);call.subscribe({next:()=>{this.message.set('Especialidad guardada.');this.close();this.load()},error:e=>this.error.set(apiError(e))})}deactivate(s:Specialty){if(!confirm(`¿Desactivar ${s.nombre}?`))return;this.api.deactivateSpecialty(s.especialidad_id).subscribe({next:()=>this.load(),error:e=>this.error.set(apiError(e))})}reactivate(s:Specialty){this.api.reactivateSpecialty(s.especialidad_id).subscribe({next:()=>this.load(),error:e=>this.error.set(apiError(e))})}close(){this.open.set(false);this.selected.set(null)}}
