import { CommonModule } from '@angular/common';import { ChangeDetectionStrategy,Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { ApiService } from '../../core/api.service';import { apiError } from '../../core/api-error';import { AuditLog } from '../../core/models';
@Component({selector:'app-audit',imports:[CommonModule,FormsModule],template:`
<section class="page-header"><div><span class="eyebrow">Trazabilidad</span><h1>Bitácora de auditoría</h1><p>Operaciones registradas automáticamente por el backend.</p></div></section>
@if(error()){<div class="alert">{{error()}}</div>}
<section class="panel list-panel"><div class="toolbar"><select [(ngModel)]="module"><option value="">Todos los módulos</option>@for(m of modules;track m){<option>{{m}}</option>}</select><select [(ngModel)]="successful"><option value="">Todos los resultados</option><option value="true">Exitosos</option><option value="false">Fallidos</option></select><button class="button secondary" (click)="load()">Filtrar</button></div>@if(loading()){<div class="loading">Cargando auditoría...</div>}@else{<div class="table-wrapper"><table><thead><tr><th>Fecha</th><th>Usuario</th><th>Acción</th><th>Módulo</th><th>Ruta</th><th>HTTP</th><th>Tiempo</th><th>Resultado</th></tr></thead><tbody>@for(a of items();track a.bitacora_id){<tr><td>{{a.fecha_evento|date:'dd/MM/yyyy HH:mm'}}</td><td>{{a.nombre_usuario||'Anónimo'}}</td><td>{{a.accion}}</td><td>{{a.modulo}}</td><td><code>{{a.ruta}}</code></td><td>{{a.codigo_respuesta}}</td><td>{{a.duracion_ms}} ms</td><td><span class="badge" [attr.data-status]="a.exitoso?'ACTIVO':'CANCELADA'">{{a.exitoso?'Exitoso':'Fallido'}}</span></td></tr>}</tbody></table></div>}</section>
`,styles:[`.toolbar{justify-content:flex-end}.toolbar select{min-height:40px;padding:0 10px;border:1px solid var(--border);border-radius:10px;background:#fff}code{font-size:.61rem;color:#344054}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class AuditComponent{private api=inject(ApiService);readonly items=signal<AuditLog[]>([]);readonly loading=signal(true);readonly error=signal('');module='';successful='';readonly modules=['PATIENTS','DOCTORS','APPOINTMENTS','CONSULTATIONS','MEDICATIONS','PRESCRIPTIONS','LAB_ORDERS','BILLING','USERS','REPORTS'];constructor(){this.load()}load(){this.loading.set(true);this.api.auditLogs(this.module,this.successful).subscribe({next:v=>{this.items.set(v.items);this.loading.set(false)},error:e=>{this.error.set(apiError(e));this.loading.set(false)}})}}
